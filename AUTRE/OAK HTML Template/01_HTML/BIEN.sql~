CREATE TABLE BIEN(
       ID_Bien VARCHAR(6); /*commencera par un B*/
       MotClef VARCHAR(30);
       Descriptif VARCHAR(180);
       LienPhoto VARCHAR(300);
       PrixNeuf FLOAT;
       Actif NUMERIC(1);
       CONSTRAINT PK_BIEN PRIMARY KEY(ID_BIEN);
);
       
