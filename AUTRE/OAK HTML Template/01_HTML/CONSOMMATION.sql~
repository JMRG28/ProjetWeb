CREATE TABLE CONSOMMATION(
       ID_Conso VARCHAR(6);
       EmailA VARCHAR(50);
       EmailB VARCHAR(50);
       ID_Consomme VARCHAR(6);
       DateDeb TIMESTAMP;
       DateFin TIMESTAMP;
       /*Verifier longueur*/
       CONSTRAINT PK_Conso PRIMARY KEY(ID_Conso);
);
